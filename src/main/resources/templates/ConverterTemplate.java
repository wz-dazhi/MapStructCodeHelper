package [(${package})];

import [(${sourceBean})];
import [(${targetBean})];
import [(${baseMapstructInterface})];
import org.mapstruct.Mapper;

/**
 * @author [(${author})]
 */
@Mapper
public interface [(${Converter})] extends [(${baseMapstruct})]<[(${source})], [(${target})]> {

}
